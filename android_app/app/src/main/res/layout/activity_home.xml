<com.adamtz.ai_sight_android.AccessibleFrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#673AB7"
    android:clickable="true"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:padding="32dp">

    <TextView
        android:id="@+id/instructionText"
        android:layout_gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="انقر للتبديل، اضغط مطولاً للاختيار"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textAlignment="center"
        android:textStyle="bold"
        android:shadowColor="#55000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="3"
        android:letterSpacing="0.05"
        android:lineSpacingExtra="8dp"
        android:fontFamily="sans-serif-medium"/>

    <!-- Add another TextView to display the current mode -->
    <TextView
        android:id="@+id/currentModeText"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Current Mode: Money"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:shadowColor="#55000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="3"
        android:letterSpacing="0.05"
        android:lineSpacingExtra="8dp"
        android:fontFamily="sans-serif-medium"/>

</com.adamtz.ai_sight_android.AccessibleFrameLayout>
